version: 2

models:
  - name: fact_observation
    columns:
      - name: observation_id
        tests:
          - not_null
          - unique
      - name: patient_id
        tests:
          - not_null
      - name: target_code
        tests:
          - not_null
